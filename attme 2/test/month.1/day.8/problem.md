# problem :
1. * `team` নামের একটি `variable` তৈরি করো এবং এর ভেতরে `bcsdevloper` String-টি ভ্যালু হিসেবে রাখো।
2. * `Positive Indexing` ব্যবহার করে `print()` ফাংশনের মাধ্যমে `String`-টির একেবারে প্রথম অক্ষরটি (অর্থাৎ `b`) আউটপুটে দেখাও।
3. * **Trick 1** ব্যবহার করে আরেকটি `print()` ফাংশনের মাধ্যমে **String**-টির একেবারে শেষের অক্ষরটি (অর্থাৎ `r`) আউটপুটে দেখাও।