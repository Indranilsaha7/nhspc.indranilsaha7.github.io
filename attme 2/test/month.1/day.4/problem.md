1. `team_name` নামের একটি `variable` তৈরি করো।

2. `input() function` ব্যবহার করে ইউজারের কাছ থেকে তার টিমের নাম জানতে চাও। (ইউজারকে স্ক্রিনে দেখানোর জন্য prompt হিসেবে `"Enter your team name: "` ব্যবহার করবে)।

3. এরপর `print() function` এবং string concatenation `(+)` ব্যবহার করে স্ক্রিনে ঠিক এই আউটপুটটি দেখাও: `"Best of luck, [টিমের নাম]!"` (অর্থাৎ ইউজার যদি ইনপুটে `bcsdevloper` দেয়, আউটপুট আসবে `Best of luck, bcsdevloper!`)।