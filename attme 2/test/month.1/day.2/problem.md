নিচের ৪টি `Variable` সঠিকভাবে তৈরি করো এবং ৪টি আলাদা `print() function` দিয়ে সেগুলোর Value স্ক্রিনে দেখাও:

1. `mission` নামের একটি String তৈরি করো, যার value হবে `NHSPC Champion`।

2. `target_year` নামের একটি Integer তৈরি করো, যার value হবে `2027`।

3. `win_rate` নামের একটি Float তৈরি করো, যার value হবে `100.0`।

4. `mission_started` নামের একটি Boolean তৈরি করো, যার value হবে `True`।